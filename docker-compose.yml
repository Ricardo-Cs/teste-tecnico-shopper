version: "1.0"

services:
  backend:
    build: 
      context: ./back-end
    ports:
      - "8080:8080"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - DATABASE_URL="postgresql://postgres:1234@localhost:5432/taxi-api?schema=public"
      - PORT=8080