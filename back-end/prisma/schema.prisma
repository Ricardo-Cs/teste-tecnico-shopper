generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model ride {
  id        String @id
  name      String
  car       String
  rating    String
  tax       Float
  kilometer Int
}
